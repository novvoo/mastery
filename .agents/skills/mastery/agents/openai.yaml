interface:
  display_name: "Mastery"
  short_description: "Repo-specific patterns and workflows for mastery"
  default_prompt: "Use the mastery repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true